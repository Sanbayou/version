
redis->linux
  src/redis-server --version
sqlserver->Windows->cmd
  sqlcmd -E -Q "SELECT @@VERSION"
postgreSQL->linux->postgres
  psql --version
rabbitMq->linux
  rabbitmqctl status | grep rabbit
oracle->linux
  su - oracle -c 'sqlplus -s / as sysdba'
